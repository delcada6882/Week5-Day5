1) Add jQuery into the project (bottom of the body tag) X
2) Add the app.js script tag under the jQuery script tag X
3) Go into app.js and select all the elements with the class of 'navItem' X
4) On each of the navItem elements change the color to white. X
5) Now select the element with the ID of 'nav' X
6) Give that element a background color of #145 X
7) Select all the even list items X
8) Change the background color to #ccc X
9) Select all the odd list items X
10) Change the background color to #aaa X
11) Select the input tag X
12) Change the value to be your first name X
13) Push this code up to github and turn in the link to your repository in canvas X